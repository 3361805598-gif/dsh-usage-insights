# 代码审查与优化记录

审查日期：2026-09-05。范围：服务端、统计归集、缓存生命周期、前端请求、构建与现有测试。依据为项目代码及本地安装的 DSH rc.2 类型声明；未读取用户会话数据。

```text
DSH 持久化会话
    → 串行队列（启动补齐 / 轮次更新 / 定期校准 / 重建）
    → 插件派生缓存（90 天明细 + 同步元数据）
    → 按本地自然日汇总
    → 同源 API → 个人分析页面
```

## 已修复

| 优先级 | 原问题及影响 | 修改 |
| --- | --- | --- |
| P1 | 重建的删除阶段没有并发锁；启动回填可覆盖实时新记录，写入失败可能形成未处理 Promise rejection | 所有缓存操作进入同一队列，合并重复重建，失败转为可见错误状态；关闭存储前等待任务退出 |
| P1 | flush 后直接遍历 live events，可能把下一轮尚未落盘的事件写入缓存 | 从 sessionPersistence.readFrom 读取已落盘事件 |
| P1 | 原生 user/message 直接携带 source，原解析只读取 data.message.source，显式技能调用漏计 | 支持原生结构并兼容旧包装结构 |
| P2 | 已报告的零 Token 被视为缺失；NaN 等异常数值反而可能视为已知 | 依据有效用量是否存在判定覆盖率 |
| P2 | 重复 finish 块重复累计；中断流回退到组装消息时丢失已有用量 | 终止块去重，组装消息缺少用量时回退到流用量 |
| P2 | 只在重新归集时过滤 90 天窗口，未变更会话的旧明细长期残留；失败无实际后台重试 | 启动清理，每分钟校准并清理；会话元数据保留用于修订号比对 |
| P2 | 仅检查源修订号，升级解析规则后历史错误统计仍可能被复用 | 增加可选 reducerVersion，兼容读取旧缓存并自动重新归集 |
| P2 | 切换窗口后的旧请求可覆盖当前结果；轮询可能重叠 | AbortController 取消旧请求，拒绝过期结果；请求完成后再安排下一轮 |
| P2 | 对每条统计事实创建 Intl.DateTimeFormat，热路径反复初始化 | 每次汇总只创建一个格式器；500 条事实测试验证创建次数为 1 |
| P2 | Host 可被 URL 解析器作为带凭据或路径的 URL 接受，Origin 接受非 Web scheme | 拒绝畸形 authority、歧义数组头、非 HTTP(S) Origin |
| P2 | npm run pack 不构建，可能把旧 lib 作为新修改打包 | prepack 自动执行 typecheck、test、build |

## 验证

- 修改前：类型检查通过，6 个测试通过。
- 修改后：`npm run check` 通过，18 个测试通过，服务端、客户端和声明文件构建成功。
- 新增回归覆盖：并发重建、实时写入顺序、落盘数据边界、过期清理、旧缓存升级、失败恢复、原生显式技能事件、零值/异常值用量、终止块去重、中断流、夏令时重复小时、格式器复用、异常请求头。
- 已同步仓库中的 lib 构建产物。
- 未在真实 DSH 页面进行浏览器交互验证，也未安装、发布或重启本机 DSH。前端取消请求与状态展示目前经过代码检查及类型/构建验证。

## 后续建议

| 顺序 | 建议 | 原因与实施条件 |
| --- | --- | --- |
| 1 | 在真实 DSH 中验证快速切换 1/7/30 天、慢请求、重建期间状态及页面卸载 | 当前没有浏览器组件测试环境，需补足宿主集成验证 |
| 2 | 已在后续前端重构中完成：完整列表展开、provider、技能结果分类 | 成功率旁已说明只计算已完成调用 |
| 3 | 大历史量场景测量后，考虑增量归集与短期汇总缓存 | 当前变更会话仍完整读取；每次 summary 仍遍历所有保留明细，虽已降低格式器开销，但未改变复杂度 |
| 4 | 增加脱敏错误类别及 API 集成测试 | 当前可看到错误状态，但缺少具体诊断；summary 的通用异常仍统一报告 invalid-time-zone |

版本号保持 0.1.1；正式发布时应更新版本并生成新安装包、校验值，避免覆盖已有发布包。

## 前端重构补充（同日）

- 重构后 `npm run check` 通过：24 个测试及类型检查、生产构建通过。
- CUA 浏览器使用模拟数据验证：三个时间范围；30 天日历点击后的 24 小时详情；8 项模型完整展开；技能结果；空覆盖率；加载错误重试恢复。
- 390px 窄屏下今日图与日历均未产生整页横向溢出；深色主题检查及浏览器日志检查通过。
- 新增测试覆盖页面加载/错误时保留范围按钮、空覆盖率、刷新失败保留旧数据、索引禁用重建、模型名称转义、缺失用量无障碍标签。
- 预览与宿主复用真实展示组件，但预览不连接真实 DSH API；真实宿主集成验证仍保留为后续事项。
